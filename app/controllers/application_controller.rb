class ApplicationController < ActionController::Base
  # CanCanCan will automatically handle unauthorized access
  # and raise an exception, which Rails will catch.
  # We don't need any special code here anymore.
end
